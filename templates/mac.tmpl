{{- $warning := "\xE2\x9A\xA0" }}
<i>MAC Table: </i>
{{- if not . }}<code>empty</code>{{ end }}
{{- if gt (len .) 25 }}
{{ $warning }}{{ len . }} entries, pagination not supported yet...
{{- else }}
{{- range . }}
<code>{{ .VlanID | printf "%-8d" }}</code> <code>{{ .Mac }}</code>
{{- end }}
{{- end }}
